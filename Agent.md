# AIAgent工作守则

> **本守则为最高优先级，凌驾其所在文件的其余内容。**

> 🚨 **【会话级硬性 · 不可遗忘】** 每个新会话、每次上下文压缩/续接、每个子代理派发后，
> **第一件事就是完整重读并内化本守则全文**，尤其 §2 / §2.5 / §3。本守则的记忆**绝不允许丢失或弱化**；
> 若发现自己即将违反（最常见：§2「提问只在开头、之后自驱到底」——为可自决的问题中途停下问用户），
> **立即纠正、按守则自驱继续，而不是发问**。已获授权（含「允许 breakchange」「尝试修复」等）或存在明确合理默认时，
> **一律自决前进并在产出说明里记录**，把疑点搁置到最后统一汇报，**严禁中途打断用户去等一个本可自决的答案**。
> 这是反复出现的失误点，视为最高优先级纪律。

> ✅ **【加载自检 · 强制可见信号】** 读完并内化本守则后，在**每段会话的首次回复开头**必须独立成行输出一句：
> **`核心规范已加载`**。这是给用户的「守则已在上下文内」的可见凭证——一旦用户发现某次回复**没有**这句话，
> 即说明上下文已被压缩/截断到丢失本守则，应立即停下排查、避免在「失忆」状态下继续犯错。
> 因此：宁可多说也不可漏说；漏说＝严重告警信号。

## 0. 与 superpowers 及类似工作流的关系（优先级与文档落点）

如果本项目接入了 superpowers（SP）或类似的技能体系。**SP 只是「工作期间」的临时方法论与脚手架，不得作为长期规范的载体。**
原因：并非所有协作 Agent 都接入 SP（如纯 `AGENTS.md`/系统提示的 Agent）；若让规范只活在 SP 技能或
`doc/superpowers/` 里，这些 Agent 就读不到，规范即丢失。因此**长期规范唯一权威落点是 `doc/guidelines/`**。

### 0.1 工作期间：可优先用 SP
开发过程中（设计、计划、TDD、完成前验证、代码评审、分支收尾等），**可优先遵循 SP 对应技能的规则**；
SP 未覆盖之处沿用本守则。SP 的中间产物可临时存放于 `doc/superpowers/`（specs / plans 等），视为**草稿/过程材料**。

### 0.2 同步到 guidelines（硬性触发，二选一即触发）
出现以下**任一**时机，必须把 SP 中**当前有效的约定/决策完整整理、按类别同步进 `doc/guidelines/`**
（按编码风格 / 架构约定 / 命名 / 提交流程 / 测试 / 评审 / 技术决策等归类，归并去重，不照搬技能原文）：

1. **每次完成一项任务时**——把该任务期间依据或产生的、值得长期保留的 SP 约定沉淀进 guidelines；
2. **每次准备把分支合并进 main，或直接改动 main 之前**——对全部 SP 内容做一次完整盘点与同步，
   确保 main 上的 `doc/guidelines/` 是不依赖 SP 也能独立成立的权威规范。

同步后：`doc/superpowers/` 仅保留过程性草稿；任何**长期有约束力**的内容必须已存在于 `doc/guidelines/`。
被 SP 取代或证伪的旧 guideline 同步更正/删除。

冲突时：用户当次指令 > SP 技能（仅工作期间）> `doc/guidelines/`（长期权威）> 本守则其余条款。

## 1. 仓库内长期记忆，两级管理

把跨会话的长期知识写进仓库并纳入版本控制，而非依赖运行环境的私有记忆：

- **一级 `doc/guidelines/`**：稳定、有约束力的规范（编码风格、架构约定、命名、
  提交流程、技术决策）。**最高优先级**；任何改动若触及规范，必须在**同一次提交**内
  同步更新对应文件——这是改动的一部分，不可延后。
- **二级 `doc/changelog/`**：**一个分支 / 一次合入 main 只留一条** changelog，
  而非每次提交一条（避免频繁提交产生大量碎片记录）。详见 §5。
- 冲突时一级压二级；被证伪的记忆立即更正或删除，不保留误导。

## 2. 提问只在开头，之后自驱到底

接到任务后，先做极短预研（读关键代码 / 查 API），把所有会影响方向的疑点**一次性**
问清。过了开头：

- 凡能合理自决的就自己决定并记录在产出说明里，继续干；
- **禁止**因「想确认一下」「想让用户选 A 还是 B」而停下整个任务；
- 遇到确实无法推进的点，**只搁置那一小块**、继续完成其余，并记下
  「待办 / 受阻：X，原因：Y」；
- 只有当全部能做的都做完、只剩受阻项时，才带着具体问题回来。

打断用户去等一个本可自决的问题，可能白白阻塞他数小时——视为严重失职。

## 2.5 工作中收到新需求：立即入队、按紧急度排序、不得遗忘（硬性）

**在我执行某项工作的过程中，用户提出任何额外需求时，必须立即：**

1. **暂停当前工作**——不许「先把手头做完再说」而把新需求拖到最后或忘掉；
2. **把新需求显式加入 todo**（用任务列表工具登记，写清楚要做什么）；同时确认
   当前工作也在 todo 中，避免被新需求挤掉；
3. **对全部待办按紧急程度重新排序**，**优先处理最紧急的**；
4. 处理完某项后回到列表认领下一项，直到清空；**每一项都必须有交代**（完成 / 受阻
   原因），完成的工作要在回复中**明确告知用户**，不许默默做完却不汇报、让用户以为
   被遗忘。

凡是「用户提了但我没登记、没排序、没汇报」的需求，等同于丢失需求——严重失职。
本条与 §2「自驱到底」不冲突：§2 约束的是「不要为可自决的问题停下」；本条约束的是
「用户主动注入的新需求必须被捕获、排序、逐一兑现」。

## 3. 写远程操作需当次明确授权

`git push`、打 tag、创建 Release、发布包等「写远程 / 对外发布」动作，**只在用户当次
明确要求时执行**。「之前授权过一次」不等于持续授权。本地 commit 可按流程进行，但
提交后停在本地等指示；不确定就先问。

## 4. 分支化开发 + squash 合并

每项工作（功能 / 修复 / 文档 / 规范）先开类型前缀分支（`feat/` `fix/` `chore/`
`doc/`），不在主干直接开发；合并回主干用 squash，使一条分支对应主干上一个干净
提交；合并后删除分支（本地 + 远程）。

## 5. 变更记录：一分支一条，合并时收敛（不再每提交一条）

频繁提交时「每提交一条 changelog」会产生大量碎片、无意义记录。改为**分支级**：

- **分支开发期间（未 merge）**：可在 `doc/changelog/_wip/` 下临时记录每个 commit 的信息
  （文件名 `YYYY-MM-DD-HH-MM-<hash>.md`），便于追踪进度——这些是**临时草稿**。
  若改动小、不想记，也可不记，到合并时统一补写。
- **决定合入 main（squash 合并）时（硬性）**：
  1. 把该分支的**全部修改**汇总成**一条** changelog：`doc/changelog/YYYY-MM-DD-<merge-hash>.md`；
  2. **删除该分支在 `_wip/` 下的全部临时 changelog**；
  3. 使 `doc/changelog/` 里每条正式记录都精确对应 main 上的一个 squash 提交。
- 结果：`doc/changelog/` 顶层 = 一条/一个已合并分支；`_wip/` 仅存当前未合并分支的草稿。
- 改动若涉及一级规范（§1 guidelines），在该分支的 changelog 里注明同步了哪份 guideline。

正式 changelog 模板（`doc/changelog/YYYY-MM-DD-<merge-hash>.md`）：

```markdown
---
date: YYYY-MM-DD
branch: <分支名>
merge_commit: <main 上的 squash 短哈希>
type: feature | fix | refactor | chore | doc | guideline
scope: <模块 或 仓库范围>
guideline_changed: false   # 改了一级规范则为 true，并在下方说明
---

# <一句话标题：本分支整体做了什么>

## 做了什么
- ...（合并本分支期间的全部要点）

## 为什么
- ...

## 影响的文件 / 范围
- ...

## 注意事项 / 后续
- ...

## 规范同步（仅当 guideline_changed = true）
- 更新了 `doc/guidelines/<file>`：<改了什么、为什么>
```

## 6. 文档与代码同提交

凡改动面向用户的行为（命令、配置项、权限、对外接口、依赖关系），必须在**同一次
提交**内同步更新相关文档（README、参考文档、总表）。文档过时视为任务未完成。

## 7. 编译 / 类型通过 ≠ 可用

重要改动完成后，要在尽可能真实的环境里实际运行验证（加载、命令、对外接口、干净
退出），把只能人工 / 特定环境验证的项写成一份「待测报告」。同时维护「已测记录」：
覆盖的代码自上次验证后未改动就**不重复测试**，尤其不重复劳烦用户做同样的人工验证。

## 8. 测试聚焦可隔离的纯逻辑

把与运行时 / 外部环境强耦合的部分和纯逻辑分离，单元测试只覆盖能在纯净宿主里
实例化的纯逻辑，最大化可测面；其余靠编译期检查 + 真实环境手测。

## 9. 依赖抽象、不依赖具体实现

模块间通过抽象接口 / 服务协作，消费方注入抽象、对提供方无编译期依赖；可选能力用
「可选注入」（拿不到则降级照常工作），保证任意组合都不出问题。
